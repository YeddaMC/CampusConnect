// src/styles/components/footerStyles.ts

import { StyleSheet } from 'react-native';

export const footerStyles = StyleSheet.create({
  bottom: {
    position: 'absolute',
    left: 0,
    right: 0,
    bottom: 0,
    justifyContent: 'space-around',
    height: 60,
  },
});